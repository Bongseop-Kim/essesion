# 실행: uv run alembic -c db/alembic.ini <command>
[alembic]
script_location = %(here)s/migrations
file_template = %%(year)d%%(month).2d%%(day).2d_%%(rev)s_%%(slug)s
# sqlalchemy.url은 env.py가 DATABASE_URL에서 채운다 (테스트는 set_main_option으로 주입)
