# @essesion/api-client

**전부 생성물** — 손으로 편집 금지. api의 OpenAPI 스펙에서 Hey API(@hey-api/openapi-ts) + TanStack Query + zod 플러그인으로 생성 (ARCHITECTURE §3).

3단계(api 1차)에서 OpenAPI 스펙 확정과 함께 코드젠을 가동하고, CI가 재생성 diff로 드리프트를 검사한다. api 스펙 변경 시 재생성해 함께 커밋할 것.
