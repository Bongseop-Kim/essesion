# E2E 02 — 디자인·토큰 (Aside) — 실행 지시서

4부작의 2편. 2026-08-04 전수 실행(`docs/reviews/design-flow-e2e-2026-08-04.md`)의 재실행 +
토큰 구매·환불. 차이는 하나: **Recraft 0회** — 지난 플랜의 S9(AI 생성)는 하지 않는다.
8/4 후속 수정 7건(`docs/reviews/design-flow-e2e-followup-2026-08.md`)의 조치 지점을 대조.

## 공통 전제

`e2e-01-store.md`의 공통 전제와 동일(서버·mock Toss·Recraft 0회·시드·리스너·중단 규칙).
결과 기록·후속은 같은 문서의 **"결과 기록과 후속"** 절을 따른다 — 결과 문서는
`docs/reviews/e2e-02-design-2026-08.md`.

토큰 회계: 첫 생성 5, 구성 수정 2, 모티프 적용·실사화·내려받기 0. 초기 지급 30은 신규 계정
기준 — `customer@local`은 기소비 상태일 수 있으므로 **D1에서 시작 잔액 X를 기록하고 이후
검산은 전부 상대값**. 소진되면 T1 토큰 구매(mock)로 충전.

## 디자인 시나리오

| ID | 단계 | 확인 |
|---|---|---|
| D0 | 비로그인 `/design` | 첫 진입 갤러리 6건, 토큰 pill placeholder, 액션 클릭 시 로그인 확인 다이얼로그(→`/login` 직행 아님) |
| D1 | 로그인 후 재진입 | 토큰 pill → **시작 잔액 X 기록**. 온보딩 다이얼로그·초기 지급 표시는 첫 진입 계정만 — 기소비면 SKIP 기록 |
| D2 | 갤러리 예시로 시작 | "토큰은 쓰지 않았어요" 스낵바, 캔버스 SVG 렌더, 타일↔평면 토글, 스크린샷 1장 |
| D3 | 새로 시작 → 첫 생성 ("굵은 대각선 줄무늬와 작은 원형 모티프가 어우러진 디자인") | SVG 갱신, 이력 카드 증가, 잔액 X→X−5 |
| D3b | 새 세션 2건 — "크림색 배경에 동백꽃을 듬성듬성 배치"(scatter), "잔잔한 네이비 페이즐리를 작은 격자로 반복"(lattice) | 흩뿌림·격자 배치 스크린샷 각 1장 |
| D4 | 구성 수정 ("줄 간격을 넓혀 주세요") | 잔액 −2, 이력 한 칸 추가, 스테퍼 되돌리기 → 이력 모달 전체 보기 |
| D5 | 슬롯 → 탐색 "벌" | 결과 그리드, "지금 쓰는 그림" 구분, 적용 후 디자인 갱신(토큰 0, Recraft 미호출) |
| D6 | 슬롯 → 글자 넣기 "영선" | 글꼴(나눔고딕↔나눔명조)·굵기 즉시 재렌더, 적용 후 내 모티프 저장 |
| D7 | 슬롯 → 사진에서 따오기 (`apps/store/public/logo/logo.png`) | 원본↔배경 제거 비교, 경고 문구, 적용 |
| D8 | 슬롯 → SVG 올리기 (`apps/worker/tests/fixtures/recraft_samples/honeybee_top.svg`) | 모달 없이 슬롯 진행 표시 → 스낵바 → 교체 |
| D9 | ~~AI 생성~~ | **SKIP — Recraft 0회. 버튼 존재·비용 안내 표시까지만, 클릭 금지** |
| D10 | 프롬프트 "고래 모티프 넣어 주세요" | 생성으로 안 가고 모티프 패널 유도 + 안내 스낵바 |
| D11 | 아이디어 모달 | 제안 목록 → 선택 시 입력창 반영 |
| D12 | 내려받기 | PNG 다운로드(크기 > 0) |
| D13 | 실사화(로컬 inline finalize — Cloud Tasks 경로는 스테이징 몫) | job 진행 → 완성본 목록 PNG, 남은 횟수 차감. **완성본은 e2e-03의 디자인 연동 주문에 쓰이니 삭제 금지** |
| D14 | 내 디자인 목록 | 세션 나열·전환·삭제, 삭제한 세션이 현재면 빈 캔버스 |
| D15 | 토큰 검산 | pill = X − (5×생성 + 2×수정) + 구매분. admin 로그의 차감·환불과 일치 |

## 토큰 시나리오

| ID | 단계 | 확인 |
|---|---|---|
| T1 | 토큰 구매 → 결제(mock) → success | 잔액 증가, `/my-page/token-history`에 구매 행, 유료/보너스/총 잔액 구분 |
| T2 | 변동 내역 | 칩 필터 4종(전체/구매·지급/사용/환불), D3~D4 사용 행 표시 |
| T3 | 토큰 환불 신청 | T1 구매 건(미사용)에서 "환불 신청" → 다이얼로그(회수 토큰·금액 명시) → 접수. 진입점 2곳 확인: 토큰 주문 상세 섹션 + token-history 카드 |
| T4 | 환불 클레임 | `/my-page/claims` 필터 "토큰 환불"에 표시 → 상세에서 "신청 취소"(전용 문구 "지급된 토큰은 그대로 유지") → 취소 후 **재신청까지 수행**(DA7의 승인 처리 대상) |
| T5 | 환불 불가 분기 | 토큰을 사용한 구매 건이면 "환불 신청" 대신 사유 문구(토큰 사용함 등) |

## admin 교차 확인

| ID | 화면 | 확인 |
|---|---|---|
| DA1 | seamless-logs | D3·D4가 저작/intent로 분류, 상세에 토큰 정산·경고·요청자 링크·세션 상관 |
| DA2 | generation-jobs | D13 finalize succeeded, 산출물 키·상세 |
| DA3 | motifs | 3필터 렌더, approved 상세의 안전 SVG 프리뷰·검토 메타. 신규 pending 없음(Recraft 미호출) — 승인 게이트 전이는 8/4 검증 완료로 SKIP |
| DA4 | design-examples | 순서 변경·게시 토글 → store 갤러리 즉시 반영(비게시 시 사라짐) 확인 |
| DA5 | few-shot-candidates·examples | 3상태 렌더, 오소링 예시 25건 목록·상세 |
| DA6 | settings·pricing | `design_edit_cost` 2→3 → store 수정 차감 3 확인(**D15 토큰 검산 이후에 수행**). pricing 값이 store 표시와 일치 |
| DA7 | customers | 토큰 잔액·이력이 D15·T1~T4와 일치, 환불 클레임 처리(승인) → store 잔액 회수 반영 |
