{
  "name": "essesion",
  "private": true,
  "packageManager": "pnpm@11.25.0",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "test:e2e": "playwright test",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "biome check . && node scripts/check-harness.mjs",
    "architecture:check": "node scripts/gc/check-docs.mjs --check && node scripts/gc/check-module-roots.mjs --check && depcruise apps/admin/src apps/store/src packages/shared/src infra/cloudflare/api-proxy/src infra/cloudflare/assets-proxy/src --config dependency-cruiser.config.mjs && uv run lint-imports --config .importlinter --no-cache",
    "format": "biome format --write .",
    "codegen": "uv run python apps/api/scripts/export_openapi.py && pnpm --filter @essesion/api-client generate"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@playwright/test": "1.62.1",
    "dependency-cruiser": "18.2.0"
  },
  "pnpm": {
    "overrides": {
      "js-yaml@<4.3.1": "5.3.0",
      "nanoid@<3.3.18": "6.0.1",
      "postcss@<8.5.23": "8.5.26",
      "sharp@<0.35.0": "0.35.3",
      "undici@<7.29.0": "8.10.0"
    }
  }
}
