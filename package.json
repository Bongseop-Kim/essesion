{
  "name": "essesion",
  "private": true,
  "packageManager": "pnpm@10.34.4",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test:e2e": "playwright test",
    "typecheck": "turbo run typecheck",
    "lint": "biome check . && node scripts/check-harness.mjs",
    "architecture:check": "node scripts/gc/check-docs.mjs --check && node scripts/gc/check-module-roots.mjs --check && depcruise apps/admin/src apps/store/src packages/shared/src infra/cloudflare/api-proxy/src --config dependency-cruiser.config.mjs && uv run lint-imports --config .importlinter --no-cache",
    "format": "biome format --write .",
    "codegen": "uv run python apps/api/scripts/export_openapi.py && pnpm --filter @essesion/api-client generate"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@playwright/test": "1.61.1",
    "dependency-cruiser": "18.2.0",
    "turbo": "^2.10.3"
  },
  "pnpm": {
    "overrides": {
      "js-yaml@<4.3.1": "4.3.1",
      "postcss@<8.5.23": "8.5.23",
      "sharp@<0.35.0": "0.35.0",
      "undici@<7.29.0": "7.29.0"
    }
  }
}
